# 项目总结：植物识别网页小游戏

本项目是一个基于 **HTML、CSS 和 JavaScript** 制作的静态网页小游戏。项目目标是让访问者通过观察网页中展示的植物图片，输入对应的植物物种名称，从而完成识别答题。

这个项目从一个普通网页开始，逐步加入图片展示、填空答题、随机抽题、计分系统、跳题处理和 GitHub Pages 发布功能，最终形成了一个可以在线访问的植物识别小游戏。

---

# 一、项目介绍

用户需要根据页面中展示的植物图片，自己判断植物名称，并在输入框中填写答案。网页会根据题库中预设的正确答案判断用户回答是否正确。

---

# 二、项目主要功能

当前版本已经实现以下功能：

## 1. 普通网页展示

网页包含：

```text
游戏标题
项目说明
植物图片展示区
答题输入区
状态提示文字
得分显示
答题进度显示
```

页面具备基础排版和响应式布局，能够在电脑和较小屏幕上正常显示。

---

## 2. 初始封面图

网页刚打开时，显示一张无关的初始图片，例如封面图或植物背景图。

此时游戏尚未开始，用户不能直接答题。

点击：

```text
开始游戏
```

后，网页才会正式随机展示植物题目图片。

---

## 3. 填空式答题

游戏最初设计过选择题模式，后来调整为填空题模式。

当前答题方式是：

```text
用户观察植物图片
↓
在输入框中输入植物名称
↓
点击“提交答案”
↓
网页判断正确或错误
```

例如正确答案是：

```text
玫瑰
```

用户输入：

```text
玫瑰
```

则判定正确。

如果输入其他内容，则判定错误，并显示正确答案。

---

## 4. 多张植物图片题库

项目支持多张植物图片。

题库写在 `script.js` 的 `questions` 数组中，每道题包含：

```text
图片路径
正确答案
```

例如：

```javascript
{
  image: "images/rose.jpg",
  answer: "玫瑰"
}
```

后续扩充题库时，只需要：

```text
把新图片放入 images 文件夹
在 questions 数组中添加对应记录
```

---

## 5. 随机抽题

每次点击“开始游戏”或“换一题”后，网页会从题库中随机抽取一张植物图片。

抽题逻辑已经优化为：

```text
一轮游戏中，题库内所有图片被抽完之前，不会重复抽题
```

也就是说，如果题库有 10 张图片，那么一轮游戏中这 10 张图片都会各出现一次，不会出现某张图片反复出现、某张图片始终没出现的问题。

---

## 6. 计分功能

游戏已经加入计分系统：

```text
答对：+1 分
答错：不加分
跳过：不加分
```

页面会实时显示：

```text
当前得分
答题进度
```

例如：

```text
当前得分：3
答题进度：5 / 10
```

---

## 7. 跳题功能

用户如果不想回答当前题，可以点击：

```text
换一题
```

此时当前题会被视为“跳过”。

跳过后：

```text
答题进度 +1
得分不增加
显示下一题
```

如果最后一题被跳过，游戏也能正常结束，并显示最终得分。

---

## 8. 游戏结束与重新开始

当一轮题库全部答完或跳过后，页面会显示最终成绩：

```text
本轮游戏结束：你的最终得分是 X / 总题数。
```

按钮会变成：

```text
重新开始
```

点击后，游戏会重置：

```text
得分归零
进度归零
重新生成题目池
开始新一轮随机抽题
```

---

# 三、项目文件结构

当前项目大致结构如下：

```text
plant-game/
├── index.html
├── style.css
├── script.js
└── images/
    ├── cover.jpg
    ├── rose.jpg
    ├── magnolia.jpg
    ├── sunflower.jpg
    └── ...
```

其中：

```text
index.html：负责网页结构
style.css：负责页面样式
script.js：负责游戏逻辑
images/：存放封面图和植物题目图片
```

---

# 四、技术组成

这个项目使用的是前端基础技术：

## HTML

用于搭建网页结构，例如：

```text
标题
图片区域
输入框
按钮
计分区域
状态提示
```

## CSS

用于美化网页，例如：

```text
页面布局
按钮样式
图片显示效果
输入框样式
答题正确或错误的颜色反馈
移动端适配
```

## JavaScript

用于实现游戏逻辑，例如：

```text
随机抽题
切换图片
读取用户输入
判断答案
更新得分
更新答题进度
处理跳题
处理游戏结束
重新开始游戏
```

## GitHub Pages

用于发布网页，使项目可以通过公网链接访问。

---

# 五、这个小游戏的玩法介绍

访问者进入网页后，会先看到游戏封面和说明。

点击“开始游戏”后，页面会随机展示一张植物图片。访问者需要观察图片，并在输入框中填写自己认为正确的植物名称。

提交答案后，网页会立即判断结果：

```text
如果回答正确，得分加 1
如果回答错误，显示正确答案
如果不想回答，可以点击“换一题”跳过
```

当所有植物图片都被答完或跳过后，系统会显示最终得分。访问者可以点击“重新开始”再次挑战。

---

# 六、项目后续可以扩展的方向

后续可以继续增强这个小游戏，例如：

```text
允许多个正确答案，例如“荷花”和“莲花”都算正确
增加答案大小写或空格容错
加入提示功能
加入倒计时功能
加入难度等级
加入题目分类，例如花卉、树木、草本植物
加入最终成绩评价
加入错题回顾
加入本地最高分记录
把题库从 script.js 分离到 plants.json
优化手机端界面
增加图片来源和版权说明
```

---

# 八、整体评价

这个项目已经从一个普通静态网页，逐步发展成了一个完整的前端交互小游戏。它虽然功能不复杂，但已经具备网页应用的基本结构：

```text
数据：植物题库
界面：图片、输入框、按钮、分数
逻辑：随机出题、判断答案、计分、结束游戏
发布：GitHub Pages 在线访问
```
